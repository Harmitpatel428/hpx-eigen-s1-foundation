node_modules
# Keep environment variables out of version control
.env
.env.local
.env.test

/generated/prisma
dist

# One-off local DB ops scripts (hold prod connection string — never commit)
.dbops/
