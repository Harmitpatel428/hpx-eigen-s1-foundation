# freshclam — keeps the signature DB current; run as root inside the container
# so build-time (root) and runtime clamd (root) agree on file ownership.
DatabaseDirectory /var/lib/clamav
DatabaseOwner root
Checks 4
LogFile /tmp/freshclam.log
PidFile /tmp/freshclam.pid
